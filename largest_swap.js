function largestSwap(num) {
	// check digits
	// if currDigits >= swapDigits
		// return true
	// else return false
	
	// reverse a string?
	
	// turn number to string
	let stringyNum = `${num}`
	console.log("stringyNum : ", stringyNum)
	console.log("stringyNum typeof : ", typeof stringyNum)
	
	// add to an array
	// add to new array in reverse order
	// combine into an integer
	
}